---
const engagements = [
  "premier atelier sous 15 jours",
  "livrable en 4 semaines",
  "votre code vous appartient",
  "données hébergées en UE",
];
---

<section aria-label="Nos engagements" class="border-b border-border bg-bg-subtle">
  <ul class="container-site grid grid-cols-1 gap-x-8 gap-y-3 py-8 sm:grid-cols-2 lg:flex lg:justify-between">
    {
      engagements.map((item) => (
        <li class="flex items-center gap-3 whitespace-nowrap font-mono text-14 text-ink">
          <span aria-hidden="true" class="text-accent">✓</span>
          {item}
        </li>
      ))
    }
  </ul>
</section>
